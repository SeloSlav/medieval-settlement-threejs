/**
 * Removed permanently: strategic humanoid substitutions violated the game's
 * exact authored-agent rendering contract. SettlementCrowdRenderer now submits
 * the original rigged GLB at every supported camera distance.
 */
export {};
