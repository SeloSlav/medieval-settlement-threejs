{
  "name": "medieval-road-system",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy:local": "spacetime publish --no-config -p ./server medieval-road-system-local -y && spacetime generate --no-config --include-private -p ./server -l typescript -o ./src/generated -y"
  },
  "devDependencies": {
    "@types/three": "^0.185.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.4"
  },
  "dependencies": {
    "spacetimedb": "2.0.1",
    "three": "^0.185.1"
  }
}
